<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <title><%= @meta_title || 'Claude Shop - AWS Bedrock配额账号' %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="UTF-8">
    <meta name="description" content="<%= @meta_description || '专业的AWS Bedrock Claude模型配额账号交易平台' %>">
    <meta name="keywords" content="<%= @meta_keywords || 'AWS Bedrock, Claude, 配额账号' %>">
    
    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="<%= @meta_title || 'Claude Shop' %>">
    <meta property="og:description" content="<%= @meta_description || '专业的AWS Bedrock Claude模型配额账号交易平台' %>">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= request.original_url %>">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Custom Styles -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <!-- Header Navigation -->
    <header class="bg-white shadow-sm sticky top-0 z-50">
      <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <!-- Logo -->
          <div class="flex items-center">
            <%= link_to public_root_path, class: "flex items-center" do %>
              <svg class="h-8 w-8 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path>
              </svg>
              <span class="ml-2 text-xl font-bold text-gray-900">Claude Shop</span>
            <% end %>
          </div>
          
          <!-- Navigation Links -->
          <div class="hidden md:flex items-center space-x-8">
            <%= link_to '首页', public_root_path, class: "text-gray-700 hover:text-indigo-600 font-medium transition" %>
            <%= link_to '账号列表', public_accounts_path, class: "text-gray-700 hover:text-indigo-600 font-medium transition" %>
            <a href="#contact" class="text-gray-700 hover:text-indigo-600 font-medium transition">联系我们</a>
            <%= link_to '管理后台', admin_root_path, class: "bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-700 transition" %>
          </div>
          
          <!-- Mobile Menu Button -->
          <div class="md:hidden">
            <button data-controller="mobile-menu" data-action="click->mobile-menu#toggle" class="text-gray-700 hover:text-indigo-600">
              <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
              </svg>
            </button>
          </div>
        </div>
        
        <!-- Mobile Menu -->
        <div data-mobile-menu-target="menu" class="hidden md:hidden py-4">
          <div class="flex flex-col space-y-2">
            <%= link_to '首页', public_root_path, class: "text-gray-700 hover:text-indigo-600 font-medium py-2" %>
            <%= link_to '账号列表', public_accounts_path, class: "text-gray-700 hover:text-indigo-600 font-medium py-2" %>
            <a href="#contact" class="text-gray-700 hover:text-indigo-600 font-medium py-2">联系我们</a>
            <%= link_to '管理后台', admin_root_path, class: "bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-700 transition inline-block text-center" %>
          </div>
        </div>
      </nav>
    </header>

    <!-- Main Content -->
    <main class="min-h-screen">
      <!-- Flash Messages -->
      <% if notice.present? %>
        <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mb-4" role="alert">
          <p><%= notice %></p>
        </div>
      <% end %>
      
      <% if alert.present? %>
        <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-4" role="alert">
          <p><%= alert %></p>
        </div>
      <% end %>
      
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white mt-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <!-- About -->
          <div>
            <h3 class="text-lg font-semibold mb-4">关于我们</h3>
            <p class="text-gray-300 text-sm">
              Claude Shop 是专业的AWS Bedrock配额账号交易平台，提供高质量的Claude模型配额账号。
            </p>
          </div>
          
          <!-- Quick Links -->
          <div>
            <h3 class="text-lg font-semibold mb-4">快速链接</h3>
            <ul class="space-y-2">
              <li><%= link_to '账号列表', public_accounts_path, class: "text-gray-300 hover:text-white text-sm" %></li>
              <li><a href="#" class="text-gray-300 hover:text-white text-sm">使用指南</a></li>
              <li><a href="#" class="text-gray-300 hover:text-white text-sm">常见问题</a></li>
            </ul>
          </div>
          
          <!-- Contact -->
          <div id="contact">
            <h3 class="text-lg font-semibold mb-4">联系方式</h3>
            <ul class="space-y-2 text-sm text-gray-300">
              <li>
                <%= telegram_general_contact_link text: 'Telegram: @claudeshop', class: 'text-gray-300 hover:text-white' %>
              </li>
              <li>Email: support@claudeshop.com</li>
            </ul>
          </div>
          
          <!-- Legal -->
          <div>
            <h3 class="text-lg font-semibold mb-4">法律信息</h3>
            <ul class="space-y-2">
              <li><a href="#" class="text-gray-300 hover:text-white text-sm">服务条款</a></li>
              <li><a href="#" class="text-gray-300 hover:text-white text-sm">隐私政策</a></li>
            </ul>
          </div>
        </div>
        
        <div class="border-t border-gray-700 mt-8 pt-8 text-center">
          <p class="text-gray-400 text-sm">
            &copy; <%= Date.current.year %> Claude Shop. All rights reserved.
          </p>
        </div>
      </div>
    </footer>

    <!-- Mobile Menu JavaScript Controller -->
    <script>
      // Stimulus controller for mobile menu
      if (typeof Stimulus !== 'undefined') {
        Stimulus.register("mobile-menu", class extends Stimulus.Controller {
          static targets = ["menu"]
          
          toggle() {
            this.menuTarget.classList.toggle("hidden")
          }
        })
      }
    </script>
  </body>
</html>